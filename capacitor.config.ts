import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'controle-estoque-mobile',
  webDir: 'www'
};

export default config;
